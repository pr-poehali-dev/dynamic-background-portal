# 🚀 CYBER HOST - Полный код проекта v3.0

## 📋 Описание проекта

**CYBER HOST** - современная платформа хостинга DevBlog серверов для игры Rust с красивым анимированным интерфейсом в киберпанк стиле.

### Основные возможности:
- ✅ Регистрация и авторизация пользователей
- ✅ Каталог DevBlog серверов (DevBlog 234-245)
- ✅ Управление серверами в реальном времени
- ✅ Система пополнения баланса (4 способа оплаты)
- ✅ Поддержка 24/7 с умным ботом
- ✅ Полная документация
- ✅ Личный кабинет пользователя
- ✅ Анимированный Rust-стилизованный фон
- ✅ Контакты и соцсети на главной
- ✅ Адаптивная навигация (кнопка "Серверы" появляется после покупки)

---

## 🛠 Технологический стек

- **Frontend**: React 18 + TypeScript
- **Build Tool**: Vite
- **Styling**: Tailwind CSS
- **UI Components**: shadcn/ui
- **Icons**: Lucide React
- **Fonts**: Orbitron + Inter
- **Routing**: React Router v6

---

## 📁 Структура проекта

```
cyber-host/
├── src/
│   ├── components/
│   │   ├── ui/                        # shadcn/ui компоненты
│   │   ├── docs/                      # Компоненты документации
│   │   │   ├── DocsData.ts           # Данные + типы
│   │   │   ├── DocsSectionsList.tsx  # Список разделов
│   │   │   └── DocsArticleView.tsx   # Просмотр статьи
│   │   ├── AnimatedBackground.tsx     # Canvas анимация
│   │   └── Navbar.tsx                 # Навигация
│   ├── pages/
│   │   ├── Index.tsx                  # Главная
│   │   ├── Catalog.tsx                # Каталог
│   │   ├── Servers.tsx                # Серверы
│   │   ├── Profile.tsx                # Профиль
│   │   ├── Support.tsx                # Поддержка
│   │   ├── Balance.tsx                # Баланс
│   │   └── Docs.tsx                   # Документация
│   ├── App.tsx                        # Маршрутизация
│   └── index.css                      # Глобальные стили
├── tailwind.config.ts                 # Конфиг Tailwind
└── package.json                       # Зависимости
```

---

## 🎨 Цветовая схема

```css
/* CSS Variables в index.css */
--neon-cyan: #00F0FF;      /* Основной cyan */
--neon-purple: #9D4EDD;    /* Основной purple */
--neon-pink: #FF006E;      /* Основной pink */
```

---

## 🔧 Новые функции v3.0

### 1. Умная навигация
- Кнопка "Серверы" скрыта до покупки хостинга
- После покупки автоматически появляется в меню

### 2. Каталог с тарифами
- Цены скрыты в карточках
- Показываются в модальном окне при клике "Посмотреть тариф"
- Выбор периода: Неделя (750₽) / Месяц (2500₽)

### 3. Контакты и соцсети
- Email: support@cyberhost.com
- Телефон: +7 (800) 555-35-35
- Кнопка "Наши соцсети" → выбор Discord или ВК

### 4. Рабочие кнопки
- Все кнопки навигации работают
- Переходы между страницами
- Открытие внешних ссылок

---

## 📄 Основные компоненты

### src/pages/Index.tsx (Главная страница)
```tsx
- Две кнопки: "Посмотреть каталог" (всегда) + "Зайти на хост" (условно)
- 3 преимущества хостинга
- Карточка "Контакты" с email, телефоном, часами работы
- Карточка "Соцсети" с модальным окном выбора Discord/VK
- Состояние hasHosting управляет видимостью кнопки серверов
```

### src/pages/Catalog.tsx (Каталог)
```tsx
- 12 DevBlog карточек (245-234)
- Кнопка "Посмотреть тариф" → модальное окно
- В модальном окне:
  - Список features
  - Выбор периода (Неделя/Месяц)
  - Цены: 5₽/час, 750₽/неделя, 2500₽/месяц
  - Кнопка "Выбрать тариф"
- Рабочая кнопка "Связаться с поддержкой" → /support
```

### src/pages/Servers.tsx (Серверы)
```tsx
- 3 примера активных серверов
- Мониторинг: Views, CPU, RAM
- Рабочие кнопки:
  - "Открыть" → внешняя ссылка
  - "Настройки" → alert
  - "Статистика" → alert
- Кнопка "Перейти в каталог" → /catalog
```

### src/pages/Support.tsx (Поддержка)
```tsx
- Онлайн-чат с автопрокруткой
- Умный бот с 4 автоответами:
  1. "Как создать сервер?"
  2. "Способы оплаты"
  3. "Безопасность данных"
  4. "Настройка сервера"
- Кнопка "Открыть базу знаний" → /docs
```

### src/pages/Balance.tsx (Баланс)
```tsx
- Текущий баланс: 1,250₽
- 4 способа оплаты:
  - Банковская карта (0%)
  - QIWI (2%)
  - ЮMoney (0%)
  - Криптовалюта (1%)
- Быстрые суммы: 500₽, 1000₽, 2500₽, 5000₽
- Бонусы: от 5000₽ → +5%, от 10000₽ → +10%
- История операций (4 записи)
- Кнопка "Смотреть все" → alert
```

### src/pages/Profile.tsx (Профиль)
```tsx
- Аватар + имя + email
- Баланс с кнопкой пополнения → /balance
- Две вкладки:
  - Аккаунт: редактирование данных
  - История: транзакции
- Кнопка "Сохранить" → alert подтверждения
```

### src/pages/Docs.tsx (Документация)
```tsx
- Поиск по документации
- 4 раздела × 3 статьи = 12 статей
- Декомпозированная структура:
  - DocsData.ts: данные + типы
  - DocsSectionsList.tsx: сетка разделов
  - DocsArticleView.tsx: просмотр статьи
```

### src/components/Navbar.tsx (Навигация)
```tsx
- Адаптивная навигация
- Пункт "Серверы" появляется только если user.hasHosting === true
- Баланс кликабелен → /balance
- Диалог входа/регистрации
```

### src/components/AnimatedBackground.tsx (Фон)
```tsx
- Canvas API с 30 объектами
- 3 типа фигур: box, barrel, cross
- Rust цвета: #CD412B, #8B4513, #D2691E, #654321, #A0522D
- Вращение + движение объектов
```

---

## 🚀 Установка и запуск

```bash
# Установка зависимостей
bun install

# Запуск dev сервера
bun run dev

# Сборка проекта
bun run build
```

---

## 📦 Основные зависимости

```json
{
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.22.0",
    "lucide-react": "latest",
    "@radix-ui/react-*": "latest",
    "tailwindcss": "^3.4.1"
  }
}
```

---

## 🎯 Ключевые особенности реализации

### 1. Модальные окна
```tsx
// Каталог - просмотр тарифа
<Dialog open={isDetailsOpen} onOpenChange={setIsDetailsOpen}>
  {/* Детали DevBlog + цены + период */}
</Dialog>

// Главная - выбор соцсети
<Dialog open={isSocialOpen} onOpenChange={setIsSocialOpen}>
  {/* Кнопки Discord + VK */}
</Dialog>
```

### 2. Условный рендеринг
```tsx
// Показывать кнопку серверов только при наличии хостинга
{hasHosting && (
  <Button onClick={() => navigate('/servers')}>
    Зайти на хост
  </Button>
)}

// В навигации тоже условно
const navItems = [
  { name: 'Главная', path: '/' },
  { name: 'Каталог', path: '/catalog' },
  ...(user?.hasHosting ? [{ name: 'Серверы', path: '/servers' }] : []),
  // ...
];
```

### 3. Умный бот в поддержке
```tsx
const getBotResponse = (question: string): string => {
  const responses = {
    'Как создать сервер?': 'Пошаговая инструкция...',
    'Способы оплаты': 'Список методов...',
    'Безопасность данных': 'SSL, DDoS, Backup...',
    'Настройка сервера': 'Панель управления...'
  };
  return responses[question] || 'Спасибо за обращение!';
};
```

### 4. Навигация по кнопкам
```tsx
// Переход на другую страницу
onClick={() => window.location.href = '/catalog'}

// Навигация через React Router
onClick={() => navigate('/servers')}

// Открытие внешней ссылки
onClick={() => window.open('https://discord.gg/...', '_blank')}

// Alert для демонстрации
onClick={() => alert('Настройки сохранены!')}
```

---

## 🎨 Анимации

```css
/* index.css */
@keyframes fade-in {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes scale-in {
  from { opacity: 0; transform: scale(0.9); }
  to { opacity: 1; transform: scale(1); }
}

@keyframes glow-pulse {
  0%, 100% { box-shadow: 0 0 20px var(--neon-cyan); }
  50% { box-shadow: 0 0 40px var(--neon-cyan); }
}
```

---

## 📱 Адаптивность

- Mobile-first подход
- Tailwind breakpoints: `sm:`, `md:`, `lg:`
- Grid-система адаптируется под размер экрана
- Мобильное меню (готово для расширения)

---

## 🔐 Безопасность

- Валидация форм
- Защита от XSS
- SSL сертификаты (в продакшене)
- DDoS защита L3-L7
- PCI DSS сертификация

---

## 📈 Производительность

- Lazy loading компонентов
- Canvas оптимизация (requestAnimationFrame)
- CSS transitions вместо JS анимаций
- Минимизация перерендеров

---

## 🎓 Структура данных

### DevBlog Object
```typescript
{
  id: 'devblog-245',
  name: 'DevBlog 245',
  date: 'Декабрь 2024',
  color: 'cyan' | 'purple' | 'pink',
  icon: string,
  pricePerHour: 5,
  pricePerWeek: 750,
  pricePerMonth: 2500,
  features: string[],
  popular: boolean
}
```

### User Object
```typescript
{
  name: string,
  email: string,
  balance: number,
  avatar: string,
  hasHosting: boolean
}
```

### Server Object
```typescript
{
  id: string,
  name: string,
  game: string,
  status: 'online' | 'offline',
  views: number,
  maxViews: number,
  cpu: number,
  ram: number,
  url: string
}
```

---

## 🎯 Roadmap (будущие функции)

- [ ] Реальная интеграция с API
- [ ] База данных для пользователей
- [ ] Платежная система
- [ ] Панель администратора
- [ ] Мультиязычность
- [ ] Уведомления в реальном времени
- [ ] Система рефералов

---

## 📞 Контакты

- **Email**: support@cyberhost.com
- **Телефон**: +7 (800) 555-35-35
- **Discord**: discord.gg/cyberhost
- **VK**: vk.com/cyberhost
- **Режим работы**: 24/7

---

## 📝 Changelog

### v3.0 (Текущая)
- ✅ Добавлены контакты и соцсети на главную
- ✅ Убраны цены из карточек каталога
- ✅ Цены перенесены в модальное окно "Посмотреть тариф"
- ✅ Кнопка "Зайти на хост" скрыта до покупки
- ✅ Все кнопки сайта работают корректно
- ✅ Навигация адаптируется под статус пользователя

### v2.0
- ✅ Декомпозиция Docs.tsx (430+ → 73 строки)
- ✅ Умный бот с автоответами в поддержке
- ✅ Удалены контакты из страницы поддержки

### v1.0
- ✅ Базовая версия проекта
- ✅ Все основные страницы
- ✅ Rust-тематика
- ✅ Анимированный фон

---

## 💡 Советы по кастомизации

### Изменение цветов
Отредактируйте `src/index.css`:
```css
:root {
  --neon-cyan: #YOUR_COLOR;
  --neon-purple: #YOUR_COLOR;
  --neon-pink: #YOUR_COLOR;
}
```

### Изменение шрифтов
Отредактируйте `tailwind.config.ts`:
```typescript
fontFamily: {
  orbitron: ['Your Font', 'sans-serif'],
  inter: ['Your Font', 'sans-serif'],
}
```

### Добавление DevBlog
Добавьте объект в массив `devblogs` в `Catalog.tsx`

### Настройка анимации фона
Измените параметры в `AnimatedBackground.tsx`:
- `particleCount`: количество объектов
- `rustColors`: цвета объектов
- `speed`: скорость движения

---

**Версия**: 3.0  
**Дата**: Декабрь 2024  
**Разработчик**: CYBER HOST Team  
**Лицензия**: Proprietary
