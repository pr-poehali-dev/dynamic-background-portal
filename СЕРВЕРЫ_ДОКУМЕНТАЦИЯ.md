# 🎮 Управление Сервером - Полная Документация

## 📋 Обзор функционала

После покупки хостинга пользователь получает доступ к полноценной панели управления сервером с возможностями:

### ✅ Основные возможности:
1. **Мониторинг в реальном времени** - CPU, RAM, игроки, аптайм
2. **Консоль сервера** - выполнение команд и просмотр логов
3. **Настройки сервера** - 2 раздела (Основные + Плагины)
4. **Перезапуск сервера** - с анимацией и логами
5. **Статистика** - детальная аналитика работы сервера
6. **Скачивание клиента** - для подключения к серверу

---

## 🎛️ Панель мониторинга

### Статистика сервера:
```typescript
{
  status: 'online',           // Статус: online / offline
  players: 24,                // Текущее кол-во игроков
  maxPlayers: 100,            // Максимум игроков (настраивается)
  cpu: 42,                    // CPU нагрузка в %
  ram: 68,                    // RAM использование в %
  uptime: '3 дня 14 часов'    // Время работы сервера
}
```

### Визуализация:
- **Progress bars** для CPU, RAM, игроков
- **Badge** со статусом (зеленый = онлайн, красный = офлайн)
- **Анимация** glow-pulse для индикатора статуса
- **Карточки** с иконками для игроков и аптайма

---

## 💻 Консоль сервера

### Функционал:
- **Просмотр логов** в реальном времени
- **Выполнение команд** через input + Enter
- **Автоскролл** к последнему сообщению
- **Цветовое кодирование** логов:
  - 🔵 `info` - голубой (информационные)
  - 🟡 `warning` - желтый (предупреждения)
  - 🔴 `error` - красный (ошибки)
  - 🟢 `success` - зеленый (успешные операции)

### Доступные команды:
```bash
help       # Показать список команд
status     # Статус сервера (игроки, CPU, RAM)
players    # Количество игроков онлайн
restart    # Перезапустить сервер
stop       # Остановить сервер
clear      # Очистить консоль
```

### Пример работы:
```
[12:34:56] Сервер успешно запущен
[12:35:10] Загрузка карты завершена
[12:35:15] Порт 28015 открыт
[12:35:20] Сервер готов к подключению игроков
> status
[12:35:25] Сервер: Online | Игроки: 24/100 | CPU: 42% | RAM: 68%
```

### Интеграция:
Все действия (настройки, перезапуск, скачивание клиента) автоматически добавляют записи в консоль.

---

## ⚙️ Настройки сервера

### Раздел 1: Основные настройки

#### 1.1 Настройки сервера:
```typescript
{
  hostname: string;           // Название сервера (отображается в списке)
  maxPlayers: number;         // 10-500 игроков
  worldSize: number;          // 1000-6000 (размер карты)
  description: string;        // Описание сервера
}
```

**Пример:**
- Название: `"CYBER HOST - PvP Arena"`
- Игроки: `150`
- Размер карты: `4000`
- Описание: `"Русский сервер с модами • x2 loot • Вайпы по средам"`

#### 1.2 Настройки карты:

**Вариант А: Кастомная карта (URL)**
```typescript
mapUrl: string;  // Прямая ссылка на .zip файл карты
```
Пример: `https://example.com/custom-map.zip`

**Вариант Б: Процедурная генерация (Seed)**
```typescript
mapSeed: string;  // Цифры для генерации (например: "1234")
```
- Кнопка **"Случайная"** - генерирует random seed
- При выборе URL очищается seed, и наоборот

**Логика работы:**
1. Если указан `mapUrl` → загружается кастомная карта
2. Если указан `mapSeed` → генерируется процедурная карта
3. Если оба пусты → используется дефолтная карта

#### Сохранение настроек:
```typescript
const handleSaveSettings = () => {
  // Сохранение в localStorage
  localStorage.setItem('activeServer', JSON.stringify({
    ...activeServer,
    settings: serverSettings
  }));
  
  // Обновление maxPlayers в статистике
  setServerStats(prev => ({ 
    ...prev, 
    maxPlayers: serverSettings.maxPlayers 
  }));
  
  // Логи в консоль
  addConsoleLog('success', 'Настройки сервера сохранены');
  addConsoleLog('info', `Название: ${serverSettings.hostname}`);
  addConsoleLog('info', `Максимум игроков: ${serverSettings.maxPlayers}`);
  
  // Уведомление пользователя
  alert('Изменения вступят в силу после перезапуска');
};
```

---

### Раздел 2: Плагины

#### Функционал:
- **Список установленных плагинов** с иконками Package
- **Добавление плагина** через input (Enter или кнопка)
- **Удаление плагина** через иконку корзины
- **Быстрый выбор** популярных плагинов

#### Популярные плагины:
```typescript
const popularPlugins = [
  'Economics',      // Экономика (деньги)
  'ServerRewards',  // Система наград
  'Kits',           // Наборы предметов
  'AdminRadar',     // Админ радар
  'BetterChat',     // Улучшенный чат
  'Vanish'          // Невидимость для админов
];
```

#### Структура плагина:
```typescript
interface ServerSettings {
  plugins: string[];  // Массив названий плагинов
}
```

#### Добавление плагина:
```typescript
const handleAddPlugin = () => {
  setServerSettings(prev => ({
    ...prev,
    plugins: [...prev.plugins, newPlugin.trim()]
  }));
  
  addConsoleLog('success', `Плагин "${newPlugin}" добавлен`);
};
```

#### Удаление плагина:
```typescript
const handleRemovePlugin = (index: number) => {
  const pluginName = serverSettings.plugins[index];
  setServerSettings(prev => ({
    ...prev,
    plugins: prev.plugins.filter((_, i) => i !== index)
  }));
  
  addConsoleLog('info', `Плагин "${pluginName}" удален`);
};
```

---

## 🔄 Перезапуск сервера

### Процесс:
```typescript
const handleRestartServer = () => {
  setIsRestarting(true);
  
  // Этап 1: Инициализация (0ms)
  addConsoleLog('warning', 'Инициализация перезапуска сервера...');
  
  // Этап 2: Остановка (500ms)
  setTimeout(() => {
    addConsoleLog('info', 'Остановка текущих процессов...');
  }, 500);
  
  // Этап 3: Сохранение (1500ms)
  setTimeout(() => {
    addConsoleLog('info', 'Сохранение данных игроков...');
  }, 1500);
  
  // Этап 4: Загрузка карты (2500ms)
  setTimeout(() => {
    addConsoleLog('info', 'Загрузка карты...');
  }, 2500);
  
  // Этап 5: Готово (3500ms)
  setTimeout(() => {
    addConsoleLog('success', 'Сервер успешно перезапущен!');
    addConsoleLog('info', `Порт ${activeServer?.port} открыт`);
    addConsoleLog('success', 'Сервер готов к подключению игроков');
    setIsRestarting(false);
  }, 3500);
};
```

### UI во время перезапуска:
- Кнопка становится **disabled**
- Иконка меняется на **Loader2** с анимацией вращения
- Текст: "Перезапуск..." вместо "Перезапуск"
- Все логи в консоли обновляются в реальном времени

---

## 📊 Статистика сервера

### Показатели:
```typescript
{
  playersOnline: 24,          // Текущие игроки
  peakPlayers: 87,            // Пиковое кол-во
  averagePlayers: 45,         // Среднее кол-во
  totalConnections: 1247,     // Всего подключений
  uptime: '3 дня 14 часов',   // Время работы
  averagePing: '28ms'         // Средний пинг
}
```

### Визуализация:
- **4 карточки** с основными метриками
- **Иконки** для каждого показателя
- **Цветовое кодирование**: cyan, purple, pink, green
- **Обновление**: каждые 60 секунд (отображается снизу)

---

## 📥 Скачивание клиента

### Содержимое файла клиента:
```
CYBER HOST - DevBlog XXX Client Launcher
=====================================================

Версия клиента: 1.0.0
Сервер: DevBlog 245
Порт: 28015
Дата активации: 08.12.2024

Инструкция по подключению:
1. Запустите Steam
2. Перейдите в библиотеку Rust
3. Нажмите Play
4. Откройте консоль (F1)
5. Введите: client.connect 127.0.0.1:28015

Название сервера: CYBER HOST Server
Максимум игроков: 100
Статус: Активен

Для прямого подключения используйте:
connect 127.0.0.1:28015

Техподдержка: support@cyberhost.com
```

### Функционал:
- **Мгновенная загрузка** через data URL
- **Динамическое имя файла**: `rust-devblog-245-client.exe`
- **Лог в консоль** при скачивании
- **Alert** с подтверждением

---

## 🔗 Информация о подключении

### Карточка с данными:
```
IP адрес:  127.0.0.1
Порт:      28015
Команда:   client.connect 127.0.0.1:28015
```

### Использование:
1. Скачать клиент
2. Скопировать команду подключения
3. Запустить Rust
4. F1 → вставить команду → Enter

---

## 💾 Сохранение данных

### localStorage структура:
```typescript
{
  activeServer: {
    devblog: 'DevBlog 245',
    core: 'rust-devblog-245-core.zip',
    client: 'rust-devblog-245-client.exe',
    port: 28015,
    purchaseDate: '2024-12-08T12:34:56.789Z',
    settings: {
      hostname: 'CYBER HOST Server',
      maxPlayers: 100,
      mapUrl: '',
      mapSeed: '1234',
      worldSize: 4000,
      description: 'Powered by CYBER HOST',
      plugins: ['Economics', 'Kits', 'BetterChat']
    }
  }
}
```

### Синхронизация:
- Настройки → localStorage → Статистика
- При обновлении `maxPlayers` в настройках автоматически обновляется в `serverStats`

---

## 🎨 UI/UX особенности

### Диалоги:
1. **Консоль** - 4xl ширина, 80vh высота
2. **Настройки** - 3xl ширина, 2 вкладки (Tabs)
3. **Статистика** - 2xl ширина, карточки с метриками

### Анимации:
- **fade-in** для карточек с задержкой
- **glow-pulse** для статуса онлайн
- **animate-spin** для иконки перезапуска
- **hover:scale-105** для кнопок

### Цветовая схема:
- **Cyan** (#00F0FF) - консоль, основные действия
- **Purple** (#9D4EDD) - настройки, плагины
- **Pink** (#FF006E) - информация, вторичные действия
- **Green** (зеленый) - успех, онлайн статус
- **Yellow** (желтый) - предупреждения
- **Red** (красный) - ошибки, удаление

---

## 🧪 Тестирование функционала

### Чек-лист:
- [ ] Мониторинг отображает правильные данные
- [ ] Консоль открывается и работают команды
- [ ] Настройки сохраняются в localStorage
- [ ] Перезапуск показывает все этапы в консоли
- [ ] Статистика отображает корректные цифры
- [ ] Клиент скачивается с правильным содержимым
- [ ] Плагины добавляются и удаляются
- [ ] Случайная карта генерирует новый seed
- [ ] Кастомная карта очищает seed
- [ ] Все кнопки работают
- [ ] Диалоги открываются/закрываются

---

## 🚀 Будущие улучшения

### Потенциальный функционал:
- [ ] Реальное WebSocket соединение с сервером
- [ ] График нагрузки CPU/RAM за последний час
- [ ] Список игроков онлайн с никами
- [ ] Система бэкапов карт
- [ ] Планировщик перезапусков (wipe schedule)
- [ ] Файловый менеджер для загрузки конфигов
- [ ] Редактор server.cfg прямо в браузере
- [ ] Магазин плагинов с описаниями
- [ ] Логи консоли с фильтрацией
- [ ] Экспорт логов в .txt файл

---

**Версия**: 1.0  
**Дата**: 08 Декабря 2024  
**Статус**: Production Ready ✅
