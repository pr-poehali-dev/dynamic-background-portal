# Техническое задание: Панель управления хостингом

## 📋 Обзор проекта

Разработка полнофункциональной панели управления для Rust серверов с системой управления плагинами, пользователями, конфигурациями и мониторингом.

---

## 🎯 Основные блоки функционала

### 1. Система управления плагинами

#### 1.1 Структура плагинов
**Текущее состояние:** Простой список плагинов с добавлением/удалением

**Новые требования:**
- Древовидная структура файловой системы сервера
- Три основных папки:
  - 📁 `plugins/` - установленные плагины (.dll, .cs файлы)
  - 📁 `config/` - конфигурационные файлы плагинов (.json, .cfg)
  - 📁 `data/` - данные плагинов (базы данных, сохранения)

**Интерфейс:**
```
🌳 Файловая структура сервера
├─ 📁 plugins/
│  ├─ 📄 Economics.cs
│  ├─ 📄 Kits.dll
│  └─ 📄 BetterChat.cs
├─ 📁 config/
│  ├─ 📄 Economics.json
│  ├─ 📄 Kits.json
│  └─ 📄 BetterChat.json
└─ 📁 data/
   ├─ 📄 economics_data.db
   └─ 📄 kits_data.json
```

#### 1.2 Редактирование конфигураций
**Функционал:**
- Клик на файл → открывается редактор кода (Monaco Editor / CodeMirror)
- Подсветка синтаксиса JSON/CFG
- Валидация JSON перед сохранением
- Кнопка "Сохранить" → отправка на сервер
- История изменений (последние 5 версий файла)

**UI компоненты:**
```typescript
<FileTree>
  <Folder name="plugins" icon="Package">
    <File name="Economics.cs" onClick={openEditor} />
  </Folder>
</FileTree>

<CodeEditor
  language="json"
  value={fileContent}
  onChange={setFileContent}
  onSave={handleSaveConfig}
/>
```

#### 1.3 Управление плагинами через консоль
**Изменения в консоли:**

**Убрать:**
- ❌ Блок "Популярные плагины" (Economics, Kits, etc.)

**Добавить команды:**
```bash
plugin.load <название>       # Загрузить плагин
plugin.reload <название>     # Перезагрузить плагин
plugin.unload <название>     # Выгрузить плагин
plugin.list                  # Список всех плагинов
```

**Автоматические логи:**
```
[12:45:30] [INFO] Загрузка плагина Economics...
[12:45:31] [SUCCESS] Плагин Economics загружен (v2.4.1)
[12:45:32] [INFO] Подключено к базе данных
[12:45:33] [SUCCESS] Economics готов к работе
```

---

### 2. Теги сервера

#### 2.1 Интерфейс управления тегами
**Расположение:** Настройки сервера → Вкладка "Теги"

**Функционал:**
- Добавление тегов (input + кнопка "Добавить")
- Удаление тегов (иконка X)
- Предустановленные теги (быстрый выбор)
- Максимум 10 тегов на сервер

**Предустановленные теги:**
```typescript
const presetTags = [
  'PvP', 'PvE', 'Solo', 'Duo', 'Trio', 'Quad', 'No Decay',
  'x2', 'x3', 'x5', 'x10', 'Vanilla', 'Modded', 'RP',
  'Русский', 'EU', 'RU', 'Weekly Wipe', 'Monthly Wipe'
];
```

**UI:**
```jsx
<TabsContent value="tags">
  <TagInput
    tags={serverSettings.tags}
    onAdd={handleAddTag}
    onRemove={handleRemoveTag}
    maxTags={10}
  />
  
  <div className="preset-tags">
    {presetTags.map(tag => (
      <Badge onClick={() => handleAddTag(tag)}>
        {tag}
      </Badge>
    ))}
  </div>
</TabsContent>
```

#### 2.2 Отображение тегов
**Вывод в логах при старте:**
```
[12:34:56] [SUCCESS] Сервер успешно запущен
[12:34:57] [INFO] Теги сервера: PvP, x2, Русский, Weekly Wipe
[12:34:58] [INFO] Порт 28015 открыт
```

**Отображение на дашборде:**
```jsx
<div className="server-tags">
  {serverSettings.tags.map(tag => (
    <Badge variant="outline">{tag}</Badge>
  ))}
</div>
```

---

### 3. Экспорт сборки сервера

#### 3.1 Кнопка экспорта
**Расположение:** Главная панель управления → рядом с "Скачать клиент"

**UI:**
```jsx
<Button onClick={handleExportBuild}>
  <Icon name="Archive" size={18} className="mr-2" />
  Скачать сборку
</Button>
```

#### 3.2 Содержимое архива
**Структура ZIP файла:**
```
cyber-host-devblog-245-build.zip
├─ server.cfg                    # Основные настройки
├─ server_description.txt        # Описание сервера
├─ tags.txt                      # Список тегов
├─ plugins/
│  ├─ Economics.cs
│  ├─ Kits.dll
│  └─ BetterChat.cs
├─ config/
│  ├─ Economics.json
│  ├─ Kits.json
│  └─ BetterChat.json
├─ data/
│  └─ (пусто - данные не экспортируются)
└─ README.txt                    # Инструкция по установке
```

#### 3.3 server.cfg содержимое
```ini
# CYBER HOST - DevBlog 245 Build
# Экспортировано: 08.12.2024 15:30:45

hostname "CYBER HOST Server"
maxplayers 100
worldsize 4000
seed 1234
port 28015

# Теги
tags "PvP,x2,Русский,Weekly Wipe"

# Описание
description "Powered by CYBER HOST | discord.gg/example"
```

#### 3.4 Процесс генерации
```typescript
const handleExportBuild = async () => {
  // 1. Показать loader
  setIsExporting(true);
  
  // 2. Собрать данные
  const buildData = {
    settings: serverSettings,
    plugins: serverSettings.plugins,
    configs: await fetchConfigs(),
    tags: serverSettings.tags,
    description: serverSettings.description
  };
  
  // 3. Отправить запрос на backend
  const response = await fetch('/api/export-build', {
    method: 'POST',
    body: JSON.stringify(buildData)
  });
  
  // 4. Получить ZIP и скачать
  const blob = await response.blob();
  downloadFile(blob, `${activeServer.devblog}-build.zip`);
  
  // 5. Лог в консоль
  addConsoleLog('success', 'Сборка сервера экспортирована');
  
  setIsExporting(false);
};
```

---

### 4. Управление пользователями

#### 4.1 Система ролей и разрешений
**Роли:**
- 👑 **Owner** - создатель сервера (все права)
- 🔧 **Admin** - администратор (все кроме удаления сервера)
- 📝 **Moderator** - модератор (консоль, просмотр логов)
- 👁️ **Viewer** - наблюдатель (только просмотр статистики)

**Разрешения:**
```typescript
interface Permissions {
  viewStats: boolean;          // Просмотр статистики
  viewConsole: boolean;        // Просмотр консоли
  executeCommands: boolean;    // Выполнение команд
  editSettings: boolean;       // Изменение настроек
  managePlugins: boolean;      // Управление плагинами
  manageUsers: boolean;        // Управление пользователями
  restartServer: boolean;      // Перезапуск сервера
  deleteServer: boolean;       // Удаление сервера (только Owner)
}
```

#### 4.2 Интерфейс добавления пользователя
**Расположение:** Настройки сервера → Вкладка "Пользователи"

**UI форма:**
```jsx
<TabsContent value="users">
  <div className="space-y-6">
    <h3>Добавить пользователя</h3>
    
    <Input
      type="email"
      placeholder="email@example.com"
      value={newUserEmail}
      onChange={e => setNewUserEmail(e.target.value)}
    />
    
    <Select value={newUserRole} onValueChange={setNewUserRole}>
      <SelectItem value="admin">🔧 Администратор</SelectItem>
      <SelectItem value="moderator">📝 Модератор</SelectItem>
      <SelectItem value="viewer">👁️ Наблюдатель</SelectItem>
    </Select>
    
    <div className="permissions-grid">
      <h4>Разрешения:</h4>
      <Checkbox id="viewStats" checked disabled>
        Просмотр статистики
      </Checkbox>
      <Checkbox id="viewConsole" checked={permissions.viewConsole}>
        Просмотр консоли
      </Checkbox>
      <Checkbox id="executeCommands" checked={permissions.executeCommands}>
        Выполнение команд
      </Checkbox>
      <Checkbox id="editSettings" checked={permissions.editSettings}>
        Изменение настроек
      </Checkbox>
      <Checkbox id="managePlugins" checked={permissions.managePlugins}>
        Управление плагинами
      </Checkbox>
    </div>
    
    <Button onClick={handleInviteUser}>
      <Icon name="Mail" className="mr-2" />
      Отправить приглашение
    </Button>
  </div>
  
  <div className="users-list mt-8">
    <h3>Текущие пользователи</h3>
    {serverUsers.map(user => (
      <UserCard
        user={user}
        onRemove={handleRemoveUser}
        onEditPermissions={handleEditPermissions}
      />
    ))}
  </div>
</TabsContent>
```

#### 4.3 Процесс добавления пользователя

**Шаг 1: Проверка авторизации**
```typescript
const handleInviteUser = async () => {
  // Проверка: залогинен ли текущий пользователь
  if (!currentUser.isAuthenticated) {
    alert('Для добавления пользователей необходимо войти в систему');
    router.push('/login');
    return;
  }
  
  // Проверка: есть ли права на добавление
  if (!currentUser.permissions.manageUsers) {
    alert('У вас нет прав на управление пользователями');
    return;
  }
  
  // Продолжаем...
};
```

**Шаг 2: Отправка приглашения**
```typescript
// Backend: /api/invite-user
const response = await fetch('/api/invite-user', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    serverPort: activeServer.port,
    invitedEmail: newUserEmail,
    role: newUserRole,
    permissions: selectedPermissions,
    invitedBy: currentUser.email
  })
});

if (response.ok) {
  addConsoleLog('success', `Приглашение отправлено на ${newUserEmail}`);
  alert(`✅ Приглашение отправлено!\n\nПользователь ${newUserEmail} получит письмо с ссылкой для подтверждения.`);
}
```

**Шаг 3: Email приглашение**
```html
Тема: Приглашение на сервер CYBER HOST

Здравствуйте!

Вас пригласил(а) user@example.com для управления сервером:
• Сервер: DevBlog 245
• Роль: Администратор
• Порт: 28015

Разрешения:
✅ Просмотр статистики
✅ Просмотр консоли
✅ Выполнение команд
✅ Изменение настроек
✅ Управление плагинами

Для принятия приглашения перейдите по ссылке:
https://cyberhost.com/accept-invite?token=abc123xyz

Ссылка действительна 7 дней.

---
CYBER HOST - Хостинг Rust серверов
```

**Шаг 4: Принятие приглашения**
```typescript
// Страница /accept-invite?token=abc123xyz

const AcceptInvite = () => {
  const [inviteData, setInviteData] = useState(null);
  
  useEffect(() => {
    // Загрузить данные приглашения
    fetch(`/api/invite-info?token=${token}`)
      .then(res => res.json())
      .then(data => setInviteData(data));
  }, []);
  
  const handleAccept = async () => {
    await fetch('/api/accept-invite', {
      method: 'POST',
      body: JSON.stringify({ token })
    });
    
    // Добавить сервер в личный кабинет
    router.push('/dashboard');
  };
  
  return (
    <Card>
      <h2>Приглашение на сервер</h2>
      <p>Сервер: {inviteData.serverName}</p>
      <p>Роль: {inviteData.role}</p>
      <Button onClick={handleAccept}>Принять приглашение</Button>
    </Card>
  );
};
```

#### 4.4 Личный кабинет пользователя
**Страница /dashboard**

```jsx
<div className="my-servers">
  <h1>Мои серверы</h1>
  
  {/* Серверы где я Owner */}
  <Section title="Мои серверы (Owner)">
    {ownedServers.map(server => (
      <ServerCard server={server} role="owner" />
    ))}
  </Section>
  
  {/* Серверы где меня пригласили */}
  <Section title="Общие серверы">
    {sharedServers.map(server => (
      <ServerCard 
        server={server} 
        role={server.myRole}
        permissions={server.myPermissions}
      />
    ))}
  </Section>
</div>

{/* Карточка сервера */}
<Card>
  <h3>{server.hostname}</h3>
  <Badge>{server.myRole}</Badge>
  <Button onClick={() => router.push(`/servers/${server.id}`)}>
    <Icon name="LogIn" className="mr-2" />
    Зайти на хостинг
  </Button>
</Card>
```

---

### 5. Аутентификация и регистрация

#### 5.1 Страница регистрации
**URL:** `/register`

```jsx
<Card className="max-w-md mx-auto">
  <h2>Регистрация</h2>
  
  <Input
    type="email"
    placeholder="Email"
    value={email}
    onChange={e => setEmail(e.target.value)}
  />
  
  <Input
    type="password"
    placeholder="Пароль"
    value={password}
    onChange={e => setPassword(e.target.value)}
  />
  
  <Input
    type="password"
    placeholder="Повторите пароль"
    value={confirmPassword}
    onChange={e => setConfirmPassword(e.target.value)}
  />
  
  <Button onClick={handleRegister}>
    Зарегистрироваться
  </Button>
  
  <p className="text-sm">
    Уже есть аккаунт? <Link to="/login">Войти</Link>
  </p>
</Card>
```

**Backend: POST /api/auth/register**
```typescript
{
  email: "user@example.com",
  password: "hashed_password",
  confirmPassword: "hashed_password"
}

// Ответ:
{
  success: true,
  message: "Письмо с подтверждением отправлено на user@example.com"
}
```

**Email подтверждения:**
```html
Тема: Подтверждение регистрации CYBER HOST

Здравствуйте!

Спасибо за регистрацию на платформе CYBER HOST.

Для подтверждения email перейдите по ссылке:
https://cyberhost.com/verify-email?token=xyz789abc

После подтверждения вы сможете:
• Создавать Rust серверы
• Управлять хостингом
• Приглашать пользователей

---
CYBER HOST
```

#### 5.2 Страница входа
**URL:** `/login`

```jsx
<Card className="max-w-md mx-auto">
  <h2>Вход</h2>
  
  <Input
    type="email"
    placeholder="Email"
    value={email}
    onChange={e => setEmail(e.target.value)}
  />
  
  <Input
    type="password"
    placeholder="Пароль"
    value={password}
    onChange={e => setPassword(e.target.value)}
  />
  
  <Button onClick={handleLogin}>
    Войти
  </Button>
  
  <div className="links">
    <Link to="/register">Регистрация</Link>
    <Link to="/forgot-password">Забыли пароль?</Link>
  </div>
</Card>
```

**Backend: POST /api/auth/login**
```typescript
{
  email: "user@example.com",
  password: "hashed_password"
}

// Ответ:
{
  success: true,
  token: "jwt_token_here",
  user: {
    id: "123",
    email: "user@example.com",
    name: "User Name",
    verified: true
  }
}
```

---

### 6. Описание сервера

#### 6.1 Поле в настройках
**Уже реализовано в:**
- `ServerSettingsDialog` → Вкладка "Основные настройки" → Textarea "Описание сервера"

**Текущий код:**
```jsx
<Textarea
  id="description"
  value={serverSettings.description}
  onChange={(e) => setServerSettings(prev => ({ 
    ...prev, 
    description: e.target.value 
  }))}
  className="bg-muted border-primary/20"
  rows={3}
  placeholder="Краткое описание вашего сервера"
/>
```

#### 6.2 Отображение на главной странице
**Добавить в `ServerDashboard`:**

```jsx
<Card className="bg-card/50 backdrop-blur-sm border border-neon-cyan/30">
  <CardHeader>
    <CardTitle className="flex items-center gap-2">
      <Icon name="FileText" size={24} className="text-neon-cyan" />
      Описание сервера
    </CardTitle>
  </CardHeader>
  <CardContent>
    {serverSettings.description ? (
      <p className="text-foreground/80 whitespace-pre-wrap">
        {serverSettings.description}
      </p>
    ) : (
      <p className="text-foreground/50 italic">
        Описание не задано
      </p>
    )}
  </CardContent>
</Card>
```

---

### 7. Внешние ссылки

#### 7.1 Раздел в настройках
**Расположение:** Настройки сервера → Вкладка "Ссылки"

**Функционал:**
- Добавление ссылок (название + URL)
- Удаление ссылок
- Иконки для популярных сервисов (Discord, VK, Telegram, YouTube, Twitch)
- Максимум 10 ссылок

**UI:**
```jsx
<TabsContent value="links">
  <div className="space-y-6">
    <h3 className="text-lg font-orbitron font-bold">
      Внешние ссылки
    </h3>
    <p className="text-sm text-foreground/60">
      Добавьте ссылки на Discord, соцсети, правила сервера и другие ресурсы.
      Игроки смогут переходить по ним.
    </p>
    
    {/* Список ссылок */}
    <div className="space-y-3">
      {serverSettings.links.map((link, index) => (
        <div key={index} className="flex items-center gap-3 p-3 bg-muted/30 rounded-lg">
          <Icon name={getIconForUrl(link.url)} size={20} />
          <div className="flex-1">
            <div className="font-semibold">{link.name}</div>
            <div className="text-sm text-foreground/60">{link.url}</div>
          </div>
          <Button
            onClick={() => handleRemoveLink(index)}
            size="sm"
            variant="outline"
            className="border-red-500/50 text-red-400 hover:bg-red-500/10"
          >
            <Icon name="Trash2" size={16} />
          </Button>
        </div>
      ))}
      
      {serverSettings.links.length === 0 && (
        <div className="p-8 text-center bg-muted/30 rounded-lg border border-dashed border-primary/20">
          <Icon name="Link" size={48} className="text-foreground/30 mx-auto mb-2" />
          <p className="text-foreground/60">Ссылки не добавлены</p>
        </div>
      )}
    </div>
    
    {/* Форма добавления */}
    {serverSettings.links.length < 10 && (
      <div className="border-t border-primary/20 pt-6 space-y-4">
        <h4>Добавить ссылку</h4>
        
        <Input
          placeholder="Название (например: Discord сервер)"
          value={newLinkName}
          onChange={e => setNewLinkName(e.target.value)}
        />
        
        <Input
          placeholder="URL (например: https://discord.gg/example)"
          value={newLinkUrl}
          onChange={e => setNewLinkUrl(e.target.value)}
        />
        
        <Button
          onClick={handleAddLink}
          className="w-full bg-gradient-to-r from-neon-cyan to-neon-purple hover:opacity-90"
        >
          <Icon name="Plus" size={18} className="mr-2" />
          Добавить ссылку
        </Button>
      </div>
    )}
    
    {/* Быстрые шаблоны */}
    <div className="p-4 bg-blue-500/10 border border-blue-500/30 rounded-lg">
      <div className="flex gap-2 mb-2">
        <Icon name="Info" size={20} className="text-blue-400" />
        <span className="font-semibold text-blue-400">Быстрые шаблоны</span>
      </div>
      <div className="grid grid-cols-2 gap-2 mt-3">
        {quickTemplates.map(template => (
          <Button
            key={template.name}
            onClick={() => {
              setNewLinkName(template.name);
              setNewLinkUrl(template.urlExample);
            }}
            size="sm"
            variant="outline"
            className="border-blue-500/50 text-blue-400 hover:bg-blue-500/10"
          >
            <Icon name={template.icon} size={16} className="mr-2" />
            {template.name}
          </Button>
        ))}
      </div>
    </div>
  </div>
</TabsContent>
```

**Быстрые шаблоны:**
```typescript
const quickTemplates = [
  { name: 'Discord', icon: 'MessageSquare', urlExample: 'https://discord.gg/' },
  { name: 'VK группа', icon: 'Users', urlExample: 'https://vk.com/club' },
  { name: 'Telegram', icon: 'Send', urlExample: 'https://t.me/' },
  { name: 'Правила', icon: 'FileText', urlExample: 'https://example.com/rules' },
  { name: 'Магазин', icon: 'ShoppingBag', urlExample: 'https://example.com/shop' },
  { name: 'YouTube', icon: 'Youtube', urlExample: 'https://youtube.com/@' }
];
```

#### 7.2 Отображение ссылок на главной странице
**Добавить в `ServerDashboard`:**

```jsx
<Card className="bg-card/50 backdrop-blur-sm border border-neon-purple/30">
  <CardHeader>
    <CardTitle className="text-xl font-orbitron text-neon-purple flex items-center gap-2">
      <Icon name="Link" size={24} />
      Полезные ссылки
    </CardTitle>
  </CardHeader>
  <CardContent className="space-y-2">
    {serverSettings.links.length === 0 ? (
      <p className="text-foreground/50 text-sm italic">
        Ссылки не добавлены
      </p>
    ) : (
      serverSettings.links.map((link, index) => (
        <a
          key={index}
          href={link.url}
          target="_blank"
          rel="noopener noreferrer"
          className="flex items-center gap-3 p-3 bg-muted/30 rounded-lg hover:bg-muted/50 transition-colors"
        >
          <Icon name={getIconForUrl(link.url)} size={20} className="text-neon-purple" />
          <div className="flex-1">
            <div className="font-semibold">{link.name}</div>
          </div>
          <Icon name="ExternalLink" size={16} className="text-foreground/50" />
        </a>
      ))
    )}
  </CardContent>
</Card>
```

**Функция определения иконки:**
```typescript
const getIconForUrl = (url: string): string => {
  if (url.includes('discord.gg') || url.includes('discord.com')) return 'MessageSquare';
  if (url.includes('vk.com')) return 'Users';
  if (url.includes('t.me') || url.includes('telegram')) return 'Send';
  if (url.includes('youtube.com') || url.includes('youtu.be')) return 'Youtube';
  if (url.includes('twitch.tv')) return 'Twitch';
  if (url.includes('rules') || url.includes('правила')) return 'FileText';
  if (url.includes('shop') || url.includes('магазин')) return 'ShoppingBag';
  return 'Link';
};
```

---

## 🔧 Технические детали

### Обновленная структура ServerSettings
```typescript
interface ServerSettings {
  // Существующие поля
  hostname: string;
  maxPlayers: number;
  mapUrl?: string;
  mapSeed?: string;
  worldSize: number;
  description: string;
  plugins: string[];
  
  // Новые поля
  tags: string[];                    // Теги сервера
  links: ServerLink[];               // Внешние ссылки
  users: ServerUser[];               // Список пользователей с доступом
  fileSystem: FileNode[];            // Древовидная структура файлов
}

interface ServerLink {
  name: string;                      // Название ссылки
  url: string;                       // URL
  createdAt: string;                 // Дата добавления
}

interface ServerUser {
  id: string;
  email: string;
  role: 'owner' | 'admin' | 'moderator' | 'viewer';
  permissions: Permissions;
  invitedBy: string;                 // Email пригласившего
  invitedAt: string;                 // Дата приглашения
  acceptedAt?: string;               // Дата принятия
}

interface FileNode {
  name: string;                      // Имя файла/папки
  type: 'file' | 'folder';
  path: string;                      // Полный путь
  content?: string;                  // Содержимое (для файлов)
  children?: FileNode[];             // Дочерние элементы (для папок)
  size?: number;                     // Размер в байтах
  modifiedAt?: string;               // Дата изменения
}
```

### Обновленная структура вкладок настроек
```typescript
<Tabs defaultValue="basic">
  <TabsList className="grid w-full grid-cols-5">
    <TabsTrigger value="basic">Основные</TabsTrigger>
    <TabsTrigger value="plugins">Плагины</TabsTrigger>
    <TabsTrigger value="tags">Теги</TabsTrigger>
    <TabsTrigger value="links">Ссылки</TabsTrigger>
    <TabsTrigger value="users">Пользователи</TabsTrigger>
  </TabsList>
  
  <TabsContent value="basic">...</TabsContent>
  <TabsContent value="plugins">...</TabsContent>
  <TabsContent value="tags">...</TabsContent>
  <TabsContent value="links">...</TabsContent>
  <TabsContent value="users">...</TabsContent>
</Tabs>
```

---

## 📦 Backend эндпоинты

### Плагины и файлы
```
GET    /api/server/:port/filesystem        # Получить структуру файлов
GET    /api/server/:port/file/:path        # Получить содержимое файла
PUT    /api/server/:port/file/:path        # Сохранить файл
POST   /api/server/:port/plugin/reload     # Перезагрузить плагин
```

### Экспорт
```
POST   /api/server/:port/export-build      # Экспортировать сборку (возвращает ZIP)
```

### Пользователи
```
POST   /api/server/:port/invite-user       # Пригласить пользователя
GET    /api/invite-info?token=:token       # Получить данные приглашения
POST   /api/accept-invite                  # Принять приглашение
DELETE /api/server/:port/user/:userId      # Удалить пользователя
PATCH  /api/server/:port/user/:userId      # Изменить права пользователя
```

### Аутентификация
```
POST   /api/auth/register                  # Регистрация
POST   /api/auth/login                     # Вход
GET    /api/auth/verify-email?token=:token # Подтверждение email
POST   /api/auth/forgot-password           # Восстановление пароля
POST   /api/auth/reset-password            # Сброс пароля
```

---

## 🎨 UI компоненты для реализации

### Новые компоненты
1. **FileTree** - древовидная структура файлов
2. **CodeEditor** - редактор кода для конфигов
3. **TagInput** - управление тегами
4. **LinkManager** - управление ссылками
5. **UserManager** - управление пользователями
6. **PermissionsGrid** - таблица разрешений

### Библиотеки
- `@monaco-editor/react` - редактор кода
- `react-folder-tree` - дерево файлов (или кастомное)
- `nodemailer` - отправка email (backend)
- `jsonwebtoken` - JWT токены (backend)

---

## ✅ Чек-лист реализации

### Этап 1: Плагины и файлы
- [ ] Создать компонент FileTree
- [ ] Интегрировать Monaco Editor
- [ ] Backend: эндпоинты для работы с файлами
- [ ] Добавить команды plugin.* в консоль
- [ ] Убрать блок "Популярные плагины"

### Этап 2: Теги и ссылки
- [ ] Создать компонент TagInput
- [ ] Добавить вкладку "Теги" в настройки
- [ ] Создать компонент LinkManager
- [ ] Добавить вкладку "Ссылки" в настройки
- [ ] Отобразить теги/ссылки на главной

### Этап 3: Экспорт
- [ ] Backend: генерация ZIP архива
- [ ] Frontend: кнопка "Скачать сборку"
- [ ] Формирование server.cfg
- [ ] Добавление README в архив

### Этап 4: Аутентификация
- [ ] Страница /register
- [ ] Страница /login
- [ ] Backend: регистрация/вход
- [ ] Email подтверждения
- [ ] JWT токены

### Этап 5: Пользователи
- [ ] Создать компонент UserManager
- [ ] Добавить вкладку "Пользователи"
- [ ] Backend: система приглашений
- [ ] Email приглашений
- [ ] Страница принятия приглашения
- [ ] Личный кабинет с общими серверами

---

**Версия ТЗ:** 1.0  
**Дата:** 08 Декабря 2024  
**Статус:** Готов к разработке ✅
